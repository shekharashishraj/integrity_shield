# Hugging Face Dataset References

```python
from datasets import load_dataset

# MBPP (coding)
ds = load_dataset("Muennighoff/mbpp", "full")

# MMLU (abstract algebra subset)
ds = load_dataset("cais/mmlu", "abstract_algebra")

# MMLU (all subjects)
ds = load_dataset("cais/mmlu", "all")

# MMLU (anatomy subset)
ds = load_dataset("cais/mmlu", "anatomy")
```

Use these snippets as quick reminders when exploring the datasets interactively.
