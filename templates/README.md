# Templates

`base_template.tex` is created automatically if missing, but you can customise it or add additional templates alongside it. Update `latex.base_template` inside `config.yaml` to point the pipeline at a new default.

Keep shared assets (logos, watermarks, etc.) in this directory so `PDFCompiler` copies them into temporary build contexts during compilation.
